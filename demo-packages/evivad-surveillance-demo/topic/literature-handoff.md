# EviVAD 文献交接

已核对 32 条核心文献的 DOI、题名、BibTeX 与本地 PDF 对应关系；详见 `paper-manifest.json` 与 `paper-mapping-audit.json`。其中 1 份超过当前 workspace 单文件 50 MiB 安全上限，保留在数据包但不作为 artifact 导入。

后续模块使用 DAA、EAD 与 DAG 三个已选研究模块。
